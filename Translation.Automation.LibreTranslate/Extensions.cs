using Translation.Automation.Core;

namespace Translation.Automation.LibreTranslate;

public static class Extensions
{
    /*public static string ToIso(this Language language)
    {
        return language switch
        {
            Language.English => "en",
            Language.Japanese => "ja",
            Language.Portuguese => "pt",
            Language.French => expr,
            Language.Chinese => expr,
            Language.German => expr,
            Language.Russian => expr,
            Language.Spanish => expr,
            Language.Vietnamese => expr,
            Language.Thai => expr,
            _ => throw new ArgumentOutOfRangeException(nameof(language), language, null)
        }
    }*/
}